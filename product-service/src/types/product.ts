export type ProductBody = {
    title: string;
    price: number;
    description: string;
};

export type Product = {
    id: string;
    title: string;
    price: number;
    description: string;
};

export type Products = Product[];
